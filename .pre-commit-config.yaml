repos:
-   repo: local
    hooks:
      - id: pytest
        name: Run pytest
        entry: uv run pytest --cache-clear
        language: system
        pass_filenames: false
        stages: [pre-push]
