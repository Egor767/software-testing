repos:
  - repo: local
    hooks:
      - id: pytest-cov
        name: pytest + coverage >=75%
        entry: uv run pytest --cov=backend --cov-report=term-missing --cov-fail-under=75 --cache-clear
        language: system
        pass_filenames: false
        stages: [pre-push]
